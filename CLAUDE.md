# CLAUDE.md

Guidance for Claude Code (claude.ai/code) working in this repository.

The complete, tool-agnostic guide — repository layout, coffea idioms (NanoEvents
modes, awkward-2 patterns, histogramming, preprocessing, processors), the
**Agent workflow** (locate → read → edit, validate like CI, guard optional
dependencies), coding conventions, and the **migration guide** across coffea's
0.7 / dask-awkward / virtual-array / pydantic eras — lives in `AGENTS.md`, which
is the single source of truth and is imported here:

@AGENTS.md

Do not add guidance to this file. Extend `AGENTS.md` instead, so every agent
(Claude Code or otherwise) reads the same instructions.
